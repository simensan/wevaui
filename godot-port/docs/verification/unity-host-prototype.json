{
  "scope": "Phase 2 of the shared-core plan: the Unity host over libweva. This receipt grows with the phase; each step records its gate as it lands.",
  "date": "2026-09-11",
  "commit": "86d2aad0",
  "step_1_plugin_build": {
    "target": "godot-port/hosts/unity (weva_core shared library; static CRT and ICU; only weva_* exported from weva_c.h and weva_unity.h)",
    "windows": {
      "toolchain": "MSVC 19.44.35222.0, Release, VS 2022 generator",
      "library": "weva_core.dll, 4,721,664 bytes",
      "library_sha256": "f06116368435b9c2360707db68e5dc71b999fea13cbdb105177e5c91f6b7c309",
      "load_test": "weva_core_load_test: abi 0.25, button at 10.0,10.0 size 29.3x22.0, 4 draws, layout probe sizes match, ok",
      "build_json": "weva_core.dll.build.json stamped (library and source digests, git state, compiler, ABI 0.25)"
    },
    "linux": {
      "toolchain": "GNU 13.3.0 (WSL Ubuntu 24.04), Release, Ninja",
      "library": "weva_core.so, 6,320,120 bytes",
      "library_sha256": "1d39f7d3fb5b27f66bf2858847d98a12aaf682fb2521338cdb7ca8885f38ed8f",
      "exports": "113 dynamic symbols, all weva_* (version script, local: *); a first build exported none because hidden visibility was applied to the core objects before the version script could pick them, fixed by scoping the preset to the plugin sources",
      "load_test": "weva_core_load_test: abi 0.25, button at 10.0,10.0 size 29.3x22.0, 4 draws, ok (identical geometry to Windows)"
    },
    "ci": "godot-ci.yml job unity-plugin (ubuntu-24.04, windows-2022): build, load test, gen_bindings --check; check.sh gate \"unity plugin\"",
    "gate": "met: the DLL loads in Unity 6000.4.1f1 EditMode and weva_abi_version() reports 0.25 (Plugin_ReportsHeaderAbiVersion)"
  },
  "step_2_bindings": {
    "generator": "godot-port/hosts/unity/gen_bindings.py -> Packages/com.wevaui/Runtime/Native/WevaNative.g.cs (113 functions, 12 structs, 7 enums)",
    "shape": "blittable: unsafe pointers, nuint for size_t, fixed buffers for arrays, delegate* unmanaged[Cdecl] callback fields, Cdecl DllImport externs; nothing marshalled, so Mono and IL2CPP share the file",
    "wrapper": "Packages/com.wevaui/Runtime/Native/NativeDocument.cs (UTF-8, status checks, ownership, draw span)",
    "editmode": {
      "unity": "6000.4.1f1, -batchmode -nographics -runTests -testPlatform EditMode -testFilter Weva.Tests.EditorTests.Native",
      "total": 18,
      "passed": 18,
      "failed": 0,
      "results": ".utmp/unity-test/native-editmode.xml",
      "cases": [
        "Plugin_ReportsHeaderAbiVersion",
        "GeneratedStruct_MatchesNativeSize x12 (weva_config, weva_vertex, weva_rounded_rect, weva_backdrop_effect, weva_draw, weva_texture, weva_render_backend, weva_glyph_bitmap, weva_font_backend, weva_shaped_glyph, weva_event, weva_binding_source)",
        "LayoutProbe_RejectsUnknownNames",
        "Document_RoundTrips (create, HTML, CSS, update, focus_next, tag name, bounds 10,10, draws with triangles, destroy)",
        "Bounds_OfNoElement_Fail",
        "Update_AfterDispose_Throws",
        "ManyDocuments_CreateAndDestroy (50)"
      ],
      "note": "rerun against the rebuilt DLL (same 18/18)"
    },
    "gate": "met: the document round trip passes from C# in EditMode and agrees with the C++ load test"
  },
  "steps_open": [
    "5 input and events",
    "6 bindings and controllers (Frontier Camp logic in a Unity script)",
    "7 layout-dump from Unity for the three-way oracle",
    "colour glyph rasterization and a real shaper for the font adapter; the in-pass (linear target) path is drawn but not pixel-verified in a scene"
  ],
  "limitations": [
    "The Unity editor is not on the CI runners; the EditMode run and the host comparison are recorded here and must be rerun by hand after header, generator or adapter changes.",
    "Windows x64 only in the package (Plugins/x86_64); the Linux .so builds and passes the load test but is not installed in the package.",
    "The plugin binary is gitignored; build it with hosts/unity (README) before the Native EditMode tests can run.",
    "The URP in-pass path composites in linear space (URP's colour buffer), so blended edges differ slightly from the gamma-composited offscreen render used for the comparison."
  ],
  "step_3_font_adapter": {
    "adapter": "Packages/com.wevaui/Runtime/Native/UnityFontBackend.cs: weva_font_backend over FontEngine (TextCore) plus the positioned shaper; faces from Font assets, files or bytes; per-code-point fallback faces encoded in the glyph id; real bold/italic variants; @font-face rules read from the core and registered (SyncCssFontFaces) through the document asset reader",
    "contract_ported_from_godot": [
      "face metrics scale the engine design units (FaceMetrics_ScaleTheEngineDesignUnits, BytesFace_MatchesTheAssetFace)",
      "glyph index and unhinted glyph metrics are the engine's own at the rounded pixel size (GlyphIndex_IsTheEngineIndex, GlyphMetrics_AreTheEngineUnhintedMetrics, GlyphMetrics_RoundThePixelSizeLikeTheGodotAdapter)",
      "coverage bitmaps: ink, top-down rows, metrics describe the bitmap, no bitmap for a space (Rasterize_*)",
      "shaping: UTF-8 byte clusters, sizing protocol, control characters, per-code-point fallback, pair adjustment, positioned form agrees with the legacy callback, 300-glyph fallback run (Shape_*, PositionedShaper_*)",
      "variants (Variant_AnswersWithRealFacesOrTheRegularOne), FontEngine state invalidation (Invalidate_ReloadsTheFaceOnTheNextCall)",
      "the core lays out with the adapter: a span is as wide as the adapter's advances, a registered family switches the face (Document_LaysOutWithTheAdapter)"
    ],
    "findings": [
      "FontEngine.GetPairAdjustmentRecord(first, second) returns an uninitialised record for a pair the face does not kern and segfaulted inside TextCore::FontEngine::GetPairAdjustmentRecord on an ordinary Inter pair (crash log in .utmp/unity-test); the adapter uses GetPairAdjustmentRecords(List<uint>) per adjacent pair instead. The C# engine's UnityFontEngineBackend.TryGetKernAdvance binds GetGlyphPairAdjustmentRecord, which answers zero for every pair on 6000.4 (its kerning is silently off); not changed in this phase.",
      "GetPairAdjustmentRecords values are design units and list a pair once per covering subtable in the font's order (Inter L-: -104 pair subtable, then -174 class subtable); the first record is what OpenType applies (-1.625px at 32px, matching fontTools). A long glyph list answered the same pair class-first, so the query is per pair.",
      "UnmanagedCallersOnlyAttribute is not in Unity 6000.4's class library (netstandard 2.1 / mono profiles checked); callbacks use MonoPInvokeCallback delegates cast to the generated function-pointer fields.",
      "LoadFontFace overloads with an int take a point size, not a face index; font collections are not supported by the adapter."
    ],
    "limitations": [
      "no ligatures or mark positioning (one glyph per code point plus pair kerning)",
      "colour glyphs are not rasterized (COLOR render mode not wired)",
      "FontEngine is process-global: the adapter reloads its face after any other user (before every update)"
    ],
    "gate": "met: 19 UnityFontBackend EditMode checks pass (part of the 42/42 Native run below)"
  },
  "step_4_render_adapter": {
    "adapter": "Packages/com.wevaui/Runtime/Native/NativeDocumentRenderer.cs + Resources/Weva-NativeMesh.shader + WevaNativeDocument (MonoBehaviour, IUINativePaintSource drawn by UIRenderPass); textures mirrored by id, consecutive same-texture draws merged into one mesh, backdrop-filter draws skipped, colour composited in gamma space like the Godot host (core colours are linear, texels sRGB)",
    "editmode": {
      "unity": "6000.4.1f1 -batchmode (graphics device present)",
      "total": 42,
      "passed": 42,
      "failed": 0,
      "results": ".utmp/unity-test/native-editmode.xml",
      "render_cases": [
        "Shader_IsAvailable",
        "Sync_BuildsBatchesOnlyWhenTheDrawListChanges",
        "Render_PutsABoxWhereTheCoreLaidItOut (pixel positions and orientation)",
        "Render_DrawsTextThroughTheAtlas",
        "FontFace_LoadsThroughTheAssetReaderAndRegistersTheFamily",
        "Parity_RendersPagesForComparison"
      ]
    },
    "comparison_with_godot": {
      "method": "godot-port/hosts/unity/compare_hosts.py (compare_render.py metric) on 1280x720 renders of the same html/css with the same font bytes (@font-face Inter); Godot via capture.tscn (opengl3, 4.7.1 + build222 extension), Unity via the Parity EditMode test; images and diff maps in .utmp/parity/<page>/",
      "frontier_camp": {
        "mean_channel_delta": 2.39,
        "worst_channel_delta": 200,
        "over_tolerance_8_pct": 2.49,
        "structural_over_64_pct": 1.08,
        "batches": 21,
        "triangles": 2064,
        "textures": 1
      },
      "randhtml": {
        "mean_channel_delta": 1.94,
        "worst_channel_delta": 226,
        "over_tolerance_8_pct": 2.21,
        "structural_over_64_pct": 1.01,
        "batches": 100,
        "triangles": 13806,
        "textures": 11
      },
      "gate_default": "compare_render.py gates at 1.0% structural; both pages sit just above it (1.08%, 1.01%) and are NOT claimed to pass that number",
      "differences_explained": [
        "text: every glyph edge differs (the diff maps are the page's text) because the two hosts rasterize with different engines (FontEngine SMOOTH vs TextServer) and position differently (TextServer quarter-pixel advances vs FontEngine fractional advances); layout geometry is identical (same core, same font bytes)",
        "colour emoji icons in randhtml are blank on Unity (colour glyphs not rasterized yet), the largest single structural cluster there",
        "1px lines and rounded-rect edges (button borders, bar underlines) differ by one row where the two rasterizers round differently",
        "the first render had a colour-space bug (vertex colours converted to linear twice, blending in linear space): mean delta 28/255, 72% over tolerance; fixed by following the Godot host (encode the core's linear colours to sRGB, blend in gamma space into a raw target)"
      ]
    },
    "gate": "met with explained differences: the Frontier Camp page and the randhtml demo render through the core in Unity and agree with the Godot host everywhere but on text rasterization and colour glyphs"
  }
}
