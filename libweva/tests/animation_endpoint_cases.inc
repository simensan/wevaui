// Captured by tools/oracle/check_animation_endpoints_chrome.cjs.
{"#a{width:20px;animation:pulse 0s linear 1s 0 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,100},
{"#a{width:20px;animation:pulse 0s linear 1s 0.25 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,125},
{"#a{width:20px;animation:pulse 0s linear 1s 1 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,200},
{"#a{width:20px;animation:pulse 0s linear 1s 1.25 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,125},
{"#a{width:20px;animation:pulse 0s linear 1s 2 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,200},
{"#a{width:20px;animation:pulse 0s linear 1s 2.25 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,125},
{"#a{width:20px;animation:pulse 0s linear 1s infinite normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,200},
{"#a{width:20px;animation:pulse 1s linear 1s 0 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,100},
{"#a{width:20px;animation:pulse 1s linear 1s 0.25 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,125},
{"#a{width:20px;animation:pulse 1s linear 1s 1 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,200},
{"#a{width:20px;animation:pulse 1s linear 1s 1.25 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,125},
{"#a{width:20px;animation:pulse 1s linear 1s 2 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,200},
{"#a{width:20px;animation:pulse 1s linear 1s 2.25 normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,125},
{"#a{width:20px;animation:pulse 1s linear 1s infinite normal both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,100},
{"#a{width:20px;animation:pulse 0s linear 1s 0 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,200},
{"#a{width:20px;animation:pulse 0s linear 1s 0.25 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,175},
{"#a{width:20px;animation:pulse 0s linear 1s 1 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,100},
{"#a{width:20px;animation:pulse 0s linear 1s 1.25 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,175},
{"#a{width:20px;animation:pulse 0s linear 1s 2 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,100},
{"#a{width:20px;animation:pulse 0s linear 1s 2.25 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,175},
{"#a{width:20px;animation:pulse 0s linear 1s infinite reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,100},
{"#a{width:20px;animation:pulse 1s linear 1s 0 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,200},
{"#a{width:20px;animation:pulse 1s linear 1s 0.25 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,175},
{"#a{width:20px;animation:pulse 1s linear 1s 1 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,100},
{"#a{width:20px;animation:pulse 1s linear 1s 1.25 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,175},
{"#a{width:20px;animation:pulse 1s linear 1s 2 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,100},
{"#a{width:20px;animation:pulse 1s linear 1s 2.25 reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,175},
{"#a{width:20px;animation:pulse 1s linear 1s infinite reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,200},
{"#a{width:20px;animation:pulse 0s linear 1s 0 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,100},
{"#a{width:20px;animation:pulse 0s linear 1s 0.25 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,125},
{"#a{width:20px;animation:pulse 0s linear 1s 1 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,200},
{"#a{width:20px;animation:pulse 0s linear 1s 1.25 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,175},
{"#a{width:20px;animation:pulse 0s linear 1s 2 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,100},
{"#a{width:20px;animation:pulse 0s linear 1s 2.25 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,125},
{"#a{width:20px;animation:pulse 0s linear 1s infinite alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,200},
{"#a{width:20px;animation:pulse 1s linear 1s 0 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,100},
{"#a{width:20px;animation:pulse 1s linear 1s 0.25 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,125},
{"#a{width:20px;animation:pulse 1s linear 1s 1 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,200},
{"#a{width:20px;animation:pulse 1s linear 1s 1.25 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,175},
{"#a{width:20px;animation:pulse 1s linear 1s 2 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,100},
{"#a{width:20px;animation:pulse 1s linear 1s 2.25 alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,125},
{"#a{width:20px;animation:pulse 1s linear 1s infinite alternate both paused}@keyframes pulse{from{width:100px}to{width:200px}}",100,200},
{"#a{width:20px;animation:pulse 0s linear 1s 0 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,200},
{"#a{width:20px;animation:pulse 0s linear 1s 0.25 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,175},
{"#a{width:20px;animation:pulse 0s linear 1s 1 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,100},
{"#a{width:20px;animation:pulse 0s linear 1s 1.25 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,125},
{"#a{width:20px;animation:pulse 0s linear 1s 2 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,200},
{"#a{width:20px;animation:pulse 0s linear 1s 2.25 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,175},
{"#a{width:20px;animation:pulse 0s linear 1s infinite alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,100},
{"#a{width:20px;animation:pulse 1s linear 1s 0 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,200},
{"#a{width:20px;animation:pulse 1s linear 1s 0.25 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,175},
{"#a{width:20px;animation:pulse 1s linear 1s 1 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,100},
{"#a{width:20px;animation:pulse 1s linear 1s 1.25 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,125},
{"#a{width:20px;animation:pulse 1s linear 1s 2 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,200},
{"#a{width:20px;animation:pulse 1s linear 1s 2.25 alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,175},
{"#a{width:20px;animation:pulse 1s linear 1s infinite alternate-reverse both paused}@keyframes pulse{from{width:100px}to{width:200px}}",200,100},
