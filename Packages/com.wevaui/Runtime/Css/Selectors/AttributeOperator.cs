namespace Weva.Css.Selectors {
    internal enum AttributeOperator {
        Exists,
        Equals,
        WhitespaceContains,
        DashMatch,
        Prefix,
        Suffix,
        Substring
    }
}
