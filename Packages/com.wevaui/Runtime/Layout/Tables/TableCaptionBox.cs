using Weva.Layout.Boxes;

namespace Weva.Layout.Tables {
    // <caption>. Per CSS Tables L3 §3.5: caption-side: top (default) places
    // the caption above the table grid, caption-side: bottom places it below.
    // Width matches the table's outer width; height is intrinsic.
    public sealed class TableCaptionBox : BlockBox {
    }
}
