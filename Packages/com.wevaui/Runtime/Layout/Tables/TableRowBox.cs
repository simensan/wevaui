using Weva.Layout.Boxes;

namespace Weva.Layout.Tables {
    // <tr>. Holds a single row's cells. The row's height = max of its cells'
    // border-box heights (CSS Tables L3 §11.6.1). Width spans the full table
    // content-box width.
    public sealed class TableRowBox : BlockBox {
    }
}
